services:
  app:
    build:
      context: .
      target: runner
    image: playvault
    container_name: playvault-app
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    env_file: .env.production
    ports:
      - "3000:3000"
